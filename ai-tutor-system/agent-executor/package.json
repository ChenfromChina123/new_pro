{
  "name": "agent-executor",
  "version": "1.0.0",
  "description": "AI Agent 执行层 - ReAct 引擎与沙箱管理",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ai": "^3.0.0",
    "strip-ansi": "^7.1.0",
    "tree-kill": "^1.2.2",
    "pino": "^8.0.0",
    "pino-pretty": "^10.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.0.0",
    "@types/tree-kill": "^1.2.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
