1、在 Vue 3 Composable 中，除非需要全局共享状态，否则应将 ref 和 reactive 变量定义在函数内部，避免多个组件实例间的数据污染。
2、使用 watchEffect 时应极其谨慎。如果在 watchEffect 内部修改了它所依赖的响应式变量（或该变量调用的函数中修改了变量），会导致死循环或非预期的重复触发。建议优先使用 watch 明确指定监听源。
3、在路由跳转时，若组件被复用（如 GamePage 路由参数变化），onMounted 可能不会重新执行或由于 store 缓存导致数据不更新。必须在 onMounted 或 watch 中增加对路由参数与当前 store 状态的匹配检查。
4、对于打字游戏类的交互，输入框的 v-model 绑定与内部状态同步逻辑必须保持单向数据流或清晰的同步机制，避免在同步过程中意外清空用户输入。
5、在 Windows 下使用 PM2 启动服务前，建议显式清理端口占用，避免 PM2 因监控僵尸进程而报错 `pidusage` 无效。
6、使用 PM2 启动脚本时，务必包含健康检查逻辑（如 `netstat` 检查端口），以确保服务真正启动成功，即使 PM2 自身状态异常。
