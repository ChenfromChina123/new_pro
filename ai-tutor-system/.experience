1、生产环境尽量使用Flyway迁移管理表结构，避免依赖ddl-auto自动建表导致差异与风险。
2、接口需按userId做强归属校验，特别是详情/删除/清空等历史数据操作，防止越权访问。
3、前端API调用尽量收敛到store/service层，避免在视图内散落直连请求，便于统一超时与错误处理。
4、子模块并入主系统时优先复用主应用鉴权与请求链路，避免维持独立启动与双Token体系。
5、微信小程序开发应封装类似 axios 的 HttpClient 类来处理请求与响应拦截，统一管理 Token 和错误跳转。
6、WXML 中的双花括号 `{{}}` 语法不支持直接使用 `||` 等复杂逻辑运算符，应使用三元运算符 `? :` 替代，否则会导致页面白屏无法渲染。
7、小程序与后端通信在开发阶段可使用局域网 IP，真机调试需注意域名合法性校验。
