Spring-Vue 智能需求分析系统：构建高效需求文档生成工具

你希望基于Spring（后端）+ Vue（前端）开发一个智能需求分析文档生成系统，核心逻辑是用户输入需求想法后，AI Agent先根据想法生成对应领域的选择题让用户作答，作答完成后Agent结合领域专家提示词（基于开源提示词库）和实时网络信息（基于开源搜索库），自动生成标准化的需求分析文档，同时前端Vue需要动态适配Agent生成的选择题UI。下面为你梳理一份结构化的需求分析文档框架，包含核心需求、技术选型、开源库推荐、核心流程和关键实现思路，可直接作为项目立项/开发的基础文档。


---
智能需求分析文档生成系统 - 需求分析文档

1. 文档概述

1.1 项目目标

基于Spring+Vue技术栈，构建一款智能化的需求分析文档生成工具，通过AI Agent模拟各领域专家，引导用户完成需求调研（选择题形式），并结合实时网络信息，自动生成符合行业规范的需求分析文档，降低需求梳理的门槛。

1.2 项目范围

模块

核心内容

用户交互

想法输入、选择题作答、文档预览/下载/导出

Agent核心

领域识别、专家提示词加载、选择题生成、实时网络搜索、文档内容生成

系统支撑

用户权限、提示词管理、文档模板管理、日志记录、接口适配

1.3 目标用户

产品经理、开发人员、初创团队成员（无专业需求分析经验的用户为主）。

2. 核心需求

2.1 功能需求

2.1.1 用户层功能

功能点

详细描述

用户注册/登录

支持手机号/账号密码登录，基础权限控制（仅查看自己生成的文档）

需求想法输入

提供富文本输入框，支持换行、简单格式（标题/列表），允许上传参考附件

动态答题交互

前端根据Agent返回的题目结构（单选/多选），自动渲染对应的答题UI，支持逐题/分页作答

文档管理

查看历史生成的文档、在线预览、下载（Word/PDF）、删除、重命名

2.1.2 Agent层功能（核心）

功能点

详细描述

领域自动识别

基于用户输入的想法，提取关键词（如“电商订单系统”“教育直播平台”），匹配预定义的领域标签（电商、教育、金融、政务等）

领域专家提示词加载

集成开源提示词库，为不同领域加载对应的“需求分析专家”提示词，让Agent具备领域专业性

选择题动态生成

Agent根据用户想法+领域提示词，生成结构化的选择题（题干、选项、类型、分值），返回前端标准化格式

实时网络信息检索

调用开源网络搜索库，根据用户需求+作答结果，检索行业最新规范、竞品信息、技术趋势等，融入文档

需求文档生成

结合用户作答结果+领域模板+实时信息，生成包含“需求背景、功能需求、非功能需求、业务流程、风险分析”等模块的标准化文档

2.1.3 系统层功能

功能点

详细描述

提示词管理

支持手动新增/编辑/删除领域提示词，同步开源提示词库的更新

文档模板管理

为不同领域提供可定制的需求分析文档模板（Word模板），支持变量填充

日志记录

记录用户操作、Agent调用、搜索请求等日志，便于问题排查

接口适配

提供RESTful接口，支持前端调用，兼容不同AI模型（如本地模型、API调用模型）

2.2 非功能需求

类型

详细要求

易用性

前端操作流程≤3步（输入想法→作答→生成文档），答题UI适配移动端/PC端

性能

Agent生成选择题≤5秒，文档生成≤10秒，支持并发用户数≥100

可扩展性

支持新增领域（仅需添加提示词+模板），支持替换AI模型/搜索库

兼容性

前端兼容Chrome/Firefox/Edge主流浏览器，文档支持Word 2016+/PDF通用格式

安全性

用户数据加密存储，文档生成后仅本人可见，接口请求做签名校验

3. 技术选型

3.1 核心技术栈

端侧

技术/框架

后端

Spring Boot 3.x、Spring Security（权限）、MyBatis-Plus（数据库）、LangChain4j（AI Agent）

前端

Vue 3 + Vite、Element Plus（UI组件）、Pinia（状态管理）、Vue Router（路由）

数据库

MySQL 8.0（用户/文档/提示词存储）、Redis（缓存Agent生成的题目/临时数据）

文档生成

Apache POI（Word生成）、iText 7（PDF生成）

3.2 关键开源库推荐（核心）

3.2.1 系统提示词开源库（Agent领域专家能力）

库名称

地址

适用场景

awesome-prompts

https://github.com/f/awesome-prompts

涵盖多领域专家提示词（产品经理、开发、设计师等），可本地化存储后按领域调用

langchain4j-prompts

https://github.com/langchain4j/langchain4j

Java生态原生适配，支持提示词模板参数化（如{{domain}} {{user_idea}}）

prompt-engineer

https://github.com/microsoft/prompt-engineer

微软开源的提示词工程库，包含行业规范类提示词，适合需求分析场景

3.2.2 网络搜索开源库（实时信息获取）

库名称

地址

适用场景

serpapi-java

https://github.com/serpapi/serpapi-java

封装Google/Bing/百度搜索API，支持结构化搜索结果（如新闻、表格、竞品信息）

langchain4j-search

langchain4j内置的SearchEngine工具

与Agent无缝集成，可直接在提示词中触发搜索，返回结果融入文档

jsoup

https://github.com/jhy/jsoup

轻量级HTML解析库，可爬取特定行业网站的公开规范（如电商行业标准）

4. 核心业务流程

暂时无法在豆包文档外展示此内容
5. 核心模块设计

5.1 后端核心模块（Spring Boot）

5.1.1 Agent核心服务（关键代码示例）

import dev.langchain4j.model.openai.OpenAiChatModel;
import dev.langchain4j.prompt.PromptTemplate;
import org.springframework.stereotype.Service;
import javax.annotation.Resource;
import java.util.HashMap;
import java.util.Map;

@Service
public class AgentService {
    // 注入OpenAI模型（也可替换为本地模型如ChatGLM）
    @Resource
    private OpenAiChatModel chatModel;
    
    // 加载开源提示词库中的“需求分析专家”提示词模板
    private static final String PROMPT_TEMPLATE = 
        "你是一名{domain}领域的资深需求分析专家，请根据用户的想法：{user_idea}，生成5-8道选择题，用于细化需求。" +
        "要求：1. 题目类型仅包含单选/多选；2. 返回JSON格式，结构为：[{\"title\":\"题干\",\"type\":\"radio/checkbox\",\"options\":[{\"label\":\"选项\",\"value\":\"值\"}]}]；" +
        "3. 题目需贴合{domain}领域的行业规范，参考最新的行业信息。";

    // 生成领域相关选择题
    public String generateQuestions(String userIdea, String domain) {
        // 1. 构建提示词参数（替换领域、用户想法）
        PromptTemplate promptTemplate = PromptTemplate.from(PROMPT_TEMPLATE);
        Map<String, String> params = new HashMap<>();
        params.put("domain", domain);
        params.put("user_idea", userIdea);
        String prompt = promptTemplate.apply(params);
        
        // 2. 调用Agent生成题目（JSON格式）
        return chatModel.generate(prompt);
    }

    // 生成需求分析文档（结合作答+实时搜索）
    public String generateDocument(String userIdea, String domain, String answers, String searchResult) {
        // 加载领域文档生成提示词，结合作答和搜索结果生成文档内容
        String docPrompt = "基于{domain}领域需求，用户想法：{user_idea}，作答结果：{answers}，最新行业信息：{searchResult}，生成一份完整的需求分析文档，包含需求背景、功能需求、非功能需求、业务流程、风险分析等模块，格式为Markdown。";
        PromptTemplate docTemplate = PromptTemplate.from(docPrompt);
        Map<String, String> docParams = new HashMap<>();
        docParams.put("domain", domain);
        docParams.put("user_idea", userIdea);
        docParams.put("answers", answers);
        docParams.put("searchResult", searchResult);
        return chatModel.generate(docTemplate.apply(docParams));
    }
}

5.1.2 网络搜索服务（关键代码示例）

import com.serpapi.SerpApiClient;
import com.serpapi.SerpApiException;
import org.springframework.stereotype.Service;
import java.util.HashMap;
import java.util.Map;

@Service
public class SearchService {
    // 初始化SerpApi客户端（替换为自己的API Key）
    private final SerpApiClient client = new SerpApiClient("你的SerpApi Key");

    // 根据关键词获取实时行业信息
    public String searchIndustryInfo(String keywords) {
        Map<String, String> params = new HashMap<>();
        params.put("q", keywords); // 搜索关键词（如“2026电商订单系统需求规范”）
        params.put("engine", "baidu"); // 指定搜索引擎（百度）
        params.put("num", "5"); // 返回5条结果
        
        try {
            // 调用搜索API，获取结构化结果
            Map<String, Object> result = client.search(params);
            // 提取关键信息并拼接（简化示例）
            return result.get("organic_results").toString();
        } catch (SerpApiException e) {
            e.printStackTrace();
            return "暂无实时信息";
        }
    }
}

5.2 前端核心模块（Vue 3）

5.2.1 动态答题组件（关键代码示例）

<template>
  <div class="answer-container">
    <!-- 动态渲染题目 -->
    <div v-for="(question, index) in questions" :key="index" class="question-item">
      <h4>{{ index + 1 }}. {{ question.title }}</h4>
      <!-- 单选题 -->
      <el-radio-group v-if="question.type === 'radio'" v-model="answers[index]">
        <el-radio 
          v-for="option in question.options" 
          :key="option.value" 
          :label="option.value"
        >
          {{ option.label }}
        </el-radio>
      </el-radio-group>
      <!-- 多选题 -->
      <el-checkbox-group v-else v-model="answers[index]">
        <el-checkbox 
          v-for="option in question.options" 
          :key="option.value" 
          :label="option.value"
        >
          {{ option.label }}
        </el-checkbox>
      </el-checkbox-group>
    </div>
    <!-- 提交按钮 -->
    <el-button type="primary" @click="submitAnswers" class="submit-btn">提交答案，生成文档</el-button>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue';
import { ElMessage } from 'element-plus';
import axios from 'axios';

// 接收父组件传递的用户想法和领域
const props = defineProps(['userIdea', 'domain']);
// 存储Agent生成的题目
const questions = ref([]);
// 存储用户答案
const answers = ref([]);

// 加载Agent生成的题目
onMounted(async () => {
  try {
    const res = await axios.post('/api/agent/generateQuestions', {
      userIdea: props.userIdea,
      domain: props.domain
    });
    // 将Agent返回的JSON字符串转为数组
    questions.value = JSON.parse(res.data);
    // 初始化答案数组
    answers.value = new Array(questions.value.length).fill('');
  } catch (error) {
    ElMessage.error('题目加载失败，请重试');
  }
});

// 提交答案，触发文档生成
const submitAnswers = async () => {
  try {
    await axios.post('/api/agent/generateDocument', {
      userIdea: props.userIdea,
      domain: props.domain,
      answers: JSON.stringify(answers.value)
    });
    ElMessage.success('文档生成成功，即将跳转预览');
    // 跳转到文档预览页
    window.location.href = '/document/preview';
  } catch (error) {
    ElMessage.error('文档生成失败，请重试');
  }
};
</script>

<style scoped>
.answer-container {
  padding: 20px;
}
.question-item {
  margin-bottom: 20px;
  padding: 10px;
  border: 1px solid #e6e6e6;
  border-radius: 8px;
}
.submit-btn {
  margin-top: 20px;
}
</style>

6. 关键难点与解决方案

难点

解决方案

Agent精准识别领域

基于IK分词提取用户想法中的关键词，匹配预定义的领域标签库（如“电商”“教育”），支持手动修正领域

动态UI适配不同题型

后端返回标准化的题目JSON结构（含type字段），前端根据type自动渲染单选/多选组件

实时信息精准融入文档

Agent生成文档前，提取“领域+需求类型+时间”关键词（如“2026教育直播平台非功能需求”），调用搜索库获取信息后填充到指定模块

文档格式标准化

为各领域提供Word模板（含占位符），Agent生成Markdown内容后，通过POI替换占位符生成标准化文档


---
总结

1. 项目核心是AI Agent+开源提示词库+网络搜索的组合，让Agent具备领域专业性和实时信息获取能力，这是区别于普通文档生成工具的关键。

2. 前后端核心适配点是标准化的题目JSON结构，前端基于该结构动态渲染UI，后端基于该结构接收作答结果，确保交互一致性。

3. 技术选型优先适配Spring+Vue生态，关键开源库（LangChain4j、serpapi、awesome-prompts）可直接集成，降低开发成本。

如果需要细化某一部分（如数据库表设计、Agent提示词模板定制、前端文档预览组件），可以告诉我，我会补充对应的详细设计和代码！
